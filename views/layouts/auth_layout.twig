<!DOCTYPE html>
<html lang="en" class="bg-white">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}NFC Attendance{% endblock %}</title>
{#    <link rel="stylesheet" href="{{ css('output.css') }}">#}
</head>
<body class="min-h-screen dark:bg-slate-800">
<div class="flex min-h-screen flex-col justify-center px-6 py-12 lg:px-8 text-gray-50">
{#    <div class="sm:mx-auto sm:w-full sm:max-w-sm">#}
{#        <a href="/"><img class="mx-auto dark:border-blue-700 h-40" src="{{ img('logo_only_white.png') }}" alt="NFC Attendance" title="Back to home"></a>#}
{#        <h2 class="mt-10 text-center text-2xl font-bold leading-9 tracking-tight text-gray-900 dark:text-white">{% block prompt %} {% endblock %}</h2>#}
{#    </div>#}
    <div class="mt-10 sm:mx-auto sm:w-full sm:max-w-sm">
        {% if app.session.getFlashMessage('success') is not empty %}
            <div class="bg-teal-100 border border-teal-200 text-sm text-teal-800 rounded-lg p-4 dark:bg-teal-800/10 dark:border-teal-900 dark:text-teal-500 mb-3 font-bold" role="alert">
                {{ app.session.getFlashMessage('success').value }}
            </div>
        {% endif %}
    </div>

    {% block content %} {% endblock %}
</div>
</body>
</html>